# milkshake API project

## scaffolding

```shell
mkdir milkshake-api && cd milkshake-api && yarn init -y
nano package.json
touch .gitignore
touch README.md
touch .env.example
mkdir config && touch config.json
```
