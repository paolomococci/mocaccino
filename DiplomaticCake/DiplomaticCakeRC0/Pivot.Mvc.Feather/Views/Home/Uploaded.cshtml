@{
    ViewData["Title"] = "DiplomaticCake";
    List<string>? items = new();
    if (ViewData["datasetList"] as List<string> != null)
    {
        items = ViewData["datasetList"] as List<string>;
    }
    else
    {
        items.Add("");
    }
}

<div class="uk-text-center uk-card-body uk-animation-scale-up uk-transform-origin-top-center">
    <h1 class="uk-heading-large uk-heading-divider">Uploaded</h1>
</div>

<div class="uk-card uk-card-default uk-card-body uk-width-1-1">
    <h3 class="uk-card-title uk-margin-large-bottom">File uploaded to the system</h3>
    <form asp-action="Process" enctype="multipart/form-data">
        <fieldset class="uk-fieldset">
            <legend class="uk-legend">please select a file:</legend>
            <div class="uk-margin">
                <select name="unique" class="uk-select uk-width-1-1 uk-form-large" aria-label="select">
                    @foreach (var item in @items!)
                    {
                        <option value="@item">@item</option>
                    }
                </select>
            </div>
            <div class="uk-margin-top">
                <button type="submit" class="uk-button uk-button-primary uk-width-1-1 uk-button-large"
                  uk-tooltip="title: sending the file containing the data to be processed; pos: bottom">
                    submit
                </button>
            </div>
        </fieldset>
    </form>
</div>
